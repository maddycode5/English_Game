Flask>=2.0,<3.0
gunicorn>=20.0
python-dotenv>=0.21.0

# The following are included as explicit requirements for clarity;
# Flask will normally pull these in as dependencies, but pinning
# them can help reproducible installs.
Werkzeug>=2.0
Jinja2>=3.0
itsdangerous>=2.0
click>=8.0
MarkupSafe>=2.0
